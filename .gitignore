__pycache__/
*.pyc
.venv/
*.egg-info/
.env
# Token and secret files (prevent accidental commits)
*.token
*.secret
*.key
.secrets
tokens.txt
secrets.txt
# Pre-commit hook should be in .git/hooks/, not committed
# (users can install it from the project root)
hooks/pre-commit.local
# Keep githooks directory in version control for distribution
# but don't track local modifications
